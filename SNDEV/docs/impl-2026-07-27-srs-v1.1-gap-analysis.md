Title: SRS v1.1 compliance gap analysis vs actual implementation
Date: 2026-07-27T00:00:00Z
Author: Seth Nenninger (tencent/hy3 Agent)
Contribution Type: Implementation
Ticket/Context: ad-hoc audit/validation task — "Determine what work remains against the SRS and actual implementation, then document for future development"
Summary: Audited BioSync-Gateway (SRS v1.1, 2026-07-24) against the current codebase. Prior v1.0 blockers (JWT env secret, DB-backed auth, hash-chain D_prev/R_i, full append-only trigger coverage, barcode d==2 rejection, FHIR persistence, symmetric microplate I/O, well-click FHIR fetch, LTTB downsampling, URS/FRS docs) are CONFIRMED FIXED in code. The dominant remaining work is deployment of the real Kitware Pulse Physiology Engine binary (PyPulse) — without it, FR-3.6 and all FR-3.11–3.14 produce deterministic synthetic physiology — plus minor items (EMA OQ-6 tolerance, dilution dead branches, DB-TLS enforcement default, uvloop/WebSocket scaling) and qualification tests that still run against a mock/absent engine. Full detail in REMAINING_WORK_v1.1.md.

---

## Task Reference

User instruction: "Determine what work remains against the SRS and actual implementation, then document for future development."

## Specification Summary

The governing spec is `SRS.md` v1.1 (2026-07-24), which added FR-3.11–FR-3.16 (PK/PD lab loop, clinical chemistry generation, digital-twin cohorts, MRD/cfDNA sandbox, LLM/RAG text gateway, integrated scenario framework) on top of the v1.0 baseline (FR-3.1–FR-3.10). A prior gap doc (`REMAINING_WORK.md`, 2026-07-22, SRS v1.0) claimed the "First 5 Actions" were completed and listed v1.0 blockers/majors/minors. This audit re-verifies those claims against current code and extends coverage to the v1.1 features.

## Implementation Notes

### Files changed (this audit)
- `REMAINING_WORK_v1.1.md` (repo root) — refreshed, SRS-v1.1-aligned gap analysis & remediation roadmap for future development.
- `SNDEV/docs/impl-2026-07-27-srs-v1.1-gap-analysis.md` — this log.

### Verification steps performed (static / code-level)
1. Read full `SRS.md` v1.1 (Sections 1–10).
2. Enumerated actual source tree (`middleware/`, `frontend/`, `database/`, `tests/`) via directory listing — confirmed new `middleware/simulation/`, `middleware/ai/`, `middleware/engine/lttb.py`, `engine/pulse_bridge.py`, `api/routes/{pkpd,chemistry,digital_twin,mrd,scenarios,ai}.py`, `alembic/versions/0007_v1_1_advanced_analytics.py`, `frontend/ScenarioDesigner/`, `frontend/src/utils/lttb.ts`, plus new test files (OQ-16/21/22/23, PQ-7/8, v1.1 schema).
3. Grep + targeted reads to verify each prior "fixed" claim and each v1.1 feature, capturing `file:line` evidence.
4. Attempted to execute pure-algorithmic unit tests; blocked because the local `.venv`/`.venv-1` do not have `numpy` installed (requirements pin `numpy==1.26.4`). Test execution therefore requires a properly provisioned environment plus a live PostgreSQL and a compiled `PyPulse` shared library.

### Evidence links (key findings)
- JWT env + fail-closed: `middleware/api/auth.py:35-63`.
- Hash chain includes D_prev + R_i: `middleware/engine/hash_chain.py:31-61`; `database/migrations/003-triggers.sql:168-173,251-256`.
- Append-only triggers on all 11 SRS §6.1 tables: `database/migrations/003-triggers.sql:44-140`.
- Barcode d<3 → critical, 8-base TruSeq, vectorized: `middleware/engine/barcode.py:73,142,158,161,258`.
- FHIR Observation/DeviceMetric persistence: `middleware/api/routes/fhir.py:114-141,198-225`.
- Pulse fails hard without PyPulse (no silent mock): `middleware/engine/pulse.py:142-153`.
- v1.1 modules synthesize deterministic data when PyPulse absent: `middleware/simulation/chemistry.py:6,20,73`; `digital_twin.py:12,61`; `mrd_sandbox.py:27,218`.
- LLM default provider = `mock`; real OpenAI-compatible routing present: `middleware/ai/llm_gateway.py:37,91-99,100,139-143`.
- LTTB wired server + client: `middleware/api/routes/telemetry.py:321`; `frontend/src/pages/TelemetryDashboard.tsx:5,148-151`.
- EMA OQ-6 tolerance still relaxed to ≤5 (SRS ≤4): `middleware/engine/signal.py:317`.
- Dilution unreachable/duplicated `elif` branches: `middleware/engine/dilution.py:315-369`.
- DB-TLS wiring present but default `sslmode=prefer`: `middleware/database.py:33-39`; `tests/middleware/tests/test_db_ssl_config.py:42-56`.

### Recommendation / verdict
Demo-complete and broadly code-complete against SRS v1.1. The single largest remaining item for true 21 CFR Part 11-grade physiological fidelity is **building and deploying the real PyPulse binary** (multi-stage `Dockerfile.pulse`) so FR-3.6 and FR-3.11–FR-3.14 run on authentic physiology instead of seeded-random synthesis. Remaining v1.0-class items are minor (EMA tolerance, dilution dead branches, DB-TLS default/enforcement, uvloop/WebSocket scaling) and qualification tests must be re-run against the real engine.
